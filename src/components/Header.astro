---
const navLinks = {
    "/projects": "Projects",
};
---

<nav>
    <div>
        <a href="/" aria-label="Home">
            Home
            <!-- <img src="/avatar.webp" alt="" width="40px" height="40px" /> -->
        </a>
        <div></div>
        {
            Object.entries(navLinks).map(([path, name]) => (
                <a href={path}>{name}</a>
            ))
        }
    </div>
</nav>
